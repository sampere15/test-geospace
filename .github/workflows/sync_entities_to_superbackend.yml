name: Sync
'on':
  schedule:
    - cron: '0 3 * * *'
  push:
    branches:
      - main
jobs:
  files:
    name: Files
    runs-on: ubuntu-latest
    steps:
      - uses: adrianjost/files-sync-action@v1
        with:
          GITHUB_TOKEN: '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'
          FILE_PATTERNS: |
            ^LICENSE\.md$
          TARGET_REPOS: |
            sampere15/test-backend-master

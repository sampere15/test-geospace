name: Sync
'on':
  schedule:
    - cron: '0 3 * * *'
  push:
    branches:
      - main
jobs:
  files:
    name: Files
    runs-on: ubuntu-latest
    steps:
      - uses: adrianjost/files-sync-action@v1
        with:
          GH_TOKEN: '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'
          FILE_PATTERNS: |
            ^LICENSE\.md$
          TARGET_REPOS: |
            sampere15/test-backend-master
